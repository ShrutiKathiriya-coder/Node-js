<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Artwork</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .form-container {
      max-width: 600px;
      margin: 50px auto;
      background: #ffffff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }
    .form-container h1 {
      margin-bottom: 30px;
      text-align: center;
    }
    .preview-image {
      display: block;
      margin: 20px auto 0;
      width: 200px;
      height: auto;
      border-radius: 8px;
    }
  </style>
</head>
<body>
  <div class="container form-container">
    <h1>Edit Artwork</h1>
    <form action="/edit/<%= artwork._id %>" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="art_title" class="form-label">Art Title</label>
        <input type="text" class="form-control" id="art_title" name="art_title" value="<%= artwork.art_title %>" required>
      </div>
      <div class="mb-3">
        <label for="artist_name" class="form-label">Artist Name</label>
        <input type="text" class="form-control" id="artist_name" name="artist_name" value="<%= artwork.artist_name %>" required>
      </div>
      <div class="mb-3">
        <label for="creation_year" class="form-label">Creation Year</label>
        <input type="number" class="form-control" id="creation_year" name="creation_year" value="<%= artwork.creation_year %>" required>
      </div>
      <div class="mb-3">
        <label for="art_type" class="form-label">Art Type</label>
        <select class="form-select" id="art_type" name="art_type" required>
          <option value="Painting" <%= artwork.art_type === 'Painting' ? 'selected' : '' %>>Painting</option>
          <option value="Sculpture" <%= artwork.art_type === 'Sculpture' ? 'selected' : '' %>>Sculpture</option>
          <option value="Photography" <%= artwork.art_type === 'Photography' ? 'selected' : '' %>>Photography</option>
          <option value="Digital" <%= artwork.art_type === 'Digital' ? 'selected' : '' %>>Digital</option>
        </select>
      </div>
      <div class="mb-3">
        <label for="art_image" class="form-label">Upload New Image</label>
        <input type="file" class="form-control" id="art_image" name="art_image">
      </div>
      <% if (artwork.art_image) { %>
        <img src="/uploads/<%= artwork.art_image %>" class="preview-image" alt="Current artwork image">
      <% } %>
      <div class="d-grid mt-4">
        <button type="submit" class="btn btn-success">Update Artwork</button>
      </div>
    </form>
  </div>
</body>
</html>