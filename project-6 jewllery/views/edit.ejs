<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Jewelry</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f9f5f1;
      font-family: 'Segoe UI', sans-serif;
    }
    .container {
      max-width: 600px;
      margin: 50px auto;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      padding: 30px;
    }
    h2 {
      text-align: center;
      color: #b58e5f;
      margin-bottom: 30px;
    }
    .preview-image {
      display: block;
      margin: 10px auto;
      max-width: 200px;
      height: auto;
      border-radius: 8px;
    }
  </style>
</head>     
<body>
  <div class="container">
    <h2>Edit Jewelry</h2>
    <form action="/edit/<%= item._id %>" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="jwelleryname" class="form-label">Jewelry Name</label>
        <input type="text" class="form-control" id="jwelleryname" name="jwelleryname" value="<%= item.jwelleryname %>" required>
      </div>
      <div class="mb-3">
        <label for="designname" class="form-label">Design Name</label>
        <input type="text" class="form-control" id="designname" name="designname" value="<%= item.designname %>" required>
      </div>
      <div class="mb-3">
        <label for="price" class="form-label">Price ($)</label>
        <input type="number" class="form-control" id="price" name="price" value="<%= item.price %>" required>
      </div>
      <div class="mb-3">
        <label for="type" class="form-label">Type</label>
        <select class="form-select" id="type" name="type" required>
          <option value="ring" <%= item.type === 'ring' ? 'selected' : '' %>>Ring</option>
          <option value="necklace" <%= item.type === 'necklace' ? 'selected' : '' %>>Necklace</option>
          <option value="bracelet" <%= item.type === 'bracelet' ? 'selected' : '' %>>Bracelet</option>
          <option value="earrings" <%= item.type === 'earrings' ? 'selected' : '' %>>Earrings</option>
        </select>
      </div>
      <div class="mb-3">
        <label for="colors" class="form-label">Colors</label>
        <input type="text" class="form-control" id="colors" name="colors" value="<%= item.colors %>">
      </div>
      <div class="mb-3">
        <label for="image" class="form-label">Upload New Image</label>
        <input type="file" class="form-control" id="image" name="image">
      </div>
      <% if (item.image) { %>
        <img src="/uploads/<%= item.image %>" class="preview-image" alt="Current jewelry image">
      <% } %>
      <button type="submit" class="btn btn-success w-100">Update Jewelry</button>
    </form>
  </div>
</body>
</html>
