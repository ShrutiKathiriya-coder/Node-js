<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Movie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <div class="container mt-4 col-6">
        <h2>Edit Movie</h2>
        <form action="/edit/<%= updatemovie._id %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="movie_title" class="form-label">Movie Title</label>
                <input type="text" class="form-control" name="movie_title" value="<%= updatemovie.movie_title %>" required>
            </div>
            <div class="mb-3">
                <label for="releaseyear" class="form-label">Release Year</label>
                <input type="text" class="form-control" name="releaseyear" value="<%= updatemovie.releaseyear %>" required>
            </div>
            <div class="mb-3">
                <label for="director" class="form-label">director name</label>
                <input type="text" class="form-control" name="director" value="<%= updatemovie.director %>" required>
            </div>
            <div class="mb-3">
                <label for="Discription" class="form-label">Discription name</label>
                <input type="text" class="form-control" name="Discription" value="<%= updatemovie.Discription %>" required>
            </div>
            <div class="mb-3">
                <label for="rating" class="form-label">rating name</label>
                <input type="number" class="form-control" name="rating" value="<%= updatemovie.rating %>" required>
            </div>
            <div class="mb-3">
                <label for="movie_type" class="form-label">movie_type</label>
                <select class="form-select" id="movie_type" name="movie_type" required>
                  <option value="horrer" <%= updatemovie.movie_type === 'horrer' ? 'selected' : '' %>>horrer</option>
                  <option value="romanse" <%= updatemovie.movie_type === 'romanse' ? 'selected' : '' %>>romanse</option>
                  <option value="comaday" <%= updatemovie.movie_type === 'comaday' ? 'selected' : '' %>>comaday</option>
                </select>
              </div>
            <div class="mb-3">
              <label for="Image" class="form-label">Upload New Image (Optional)</label>
              <input type="file" class="form-control" name="Image">
              <% if (updatemovie.Image) { %>
                  <img src="/uploads/<%= updatemovie.Image %>" alt="Movie Image" width="100">
              <% } %>
            </div>
            <button type="submit" class="btn btn-primary">Save Changes</button>
            </div>
        </form>
    </div>

</body>
</html>
